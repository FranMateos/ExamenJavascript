var assert = chai.assert;
var expect = chai.expect;

suite("Ejemplo de bateria de tests", function() {
    test("Test unitario", function() {
        expect(validate("prueba")).to.equal("salida");
    });
 

});


